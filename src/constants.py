import os
from pathlib import Path

PROJECT_HOME = Path(__file__).parent.parent
ASSETS_DIR = PROJECT_HOME / "assets"
INPUT_DIR = ASSETS_DIR / "input" / "assets"
OUTPUT_DIR = ASSETS_DIR / "output" / "assets"
